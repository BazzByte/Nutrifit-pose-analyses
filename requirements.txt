# ============================================================
# fitness_ai — متطلبات المشروع الكاملة
# fitness_ai — Full project requirements
# ============================================================

# كشف الوضعية / Pose detection
mediapipe>=0.10.0

# معالجة الصور والكاميرا / Image & camera processing
opencv-python>=4.8.0

# حسابات رياضية / Math & arrays
numpy>=1.24.0

# دعم النص العربي والخطوط / Arabic text & font rendering
Pillow>=10.0.0
arabic-reshaper>=3.0.0
python-bidi>=0.4.2

# خادم HTTP للتكامل مع Flutter / HTTP server for Flutter integration
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# اختبارات / Testing
pytest>=7.4.0
pytest-cov>=4.1.0
